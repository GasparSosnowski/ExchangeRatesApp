<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Schema" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/myBlack</item>
        <item name="colorPrimaryVariant">@color/myBlack</item>
        <item name="colorOnPrimary">@color/myBlack</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/myBlack</item>
        <item name="colorSecondaryVariant">@color/myBlack</item>
        <item name="colorOnSecondary">@color/myBlack</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>